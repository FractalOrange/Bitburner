/** @param {NS} ns **/
export async function main(ns) {
    // Need to put in 2 locations for each city

    // This is super slow

    // This interrupts focus.

	let places = ["National Security Agency", "Joe's Guns"]
	ns.tail("intelligenceFarm.js", "home")
	while (true == true) {
		for (let place of places){
			ns.goToLocation(place)
			ns.goToLocation
		}
		await ns.sleep(1)
	}

}